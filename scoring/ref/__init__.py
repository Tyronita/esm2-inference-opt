# scoring/ref/ — Track A: refs/fair-esm @ 2b369911, fp32
# This is the REFERENCE implementation used for all benchmark runs.
# API: score_variants(model, alphabet, sequence, variants, device)
# where model, alphabet = fair_esm.pretrained.esm2_t33_650M_UR50D()
